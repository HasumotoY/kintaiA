<% provide(:title,@user.name) %>
<% provide(:class_text,"edit-attendaces") %>
<%= form_with(model: @attendances, url: attendances_update_one_month_user_path(@attendances), local: true, method: :patch) do |f| %>
<%= render 'shared/error_messages' %>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-condenced table-bordered table-hover table-striped" id="table-edit-attendances">
    <thead>
      <tr>
        <td>日付</td>
        <td>曜日</td>
        <td>出社</td>
        <td>退社</td>
        <td>翌日</td>
        <td>在社時間合計</td>
        <td>備考</td>
        <td>指示者確認㊞</td>
      </tr> 
    </thead>
      <% @attendances.each do |day| %>
        
          <tbody>
            <tr>
              <td><%= l(day.worked_on, format: :short) %></td>
      
              <td>
                <% if day.worked_on.wday == 6 %>
                  <font color="blue">土</font>
                <% elsif day.worked_on.wday == 0 %>
                  <font color="red">日</font>
                <% else %>
                  <%= $days_of_the_week[day.worked_on.wday] %>
                <% end %>
              </td>
              
              <% if Date.current <= day.worked_on %>
                <td><%= f.time_field :started_at, readonly: true, class: "form-control" %></td>
                <td><%= f.time_field :finished_at,readonly: true, class: "form-control" %></td>
              <% else %>
                <td><%= f.time_field :started_at, class: "form-control" %></td>
                <td><%= f.time_field :finished_at, class: "form-control" %></td>
              <% end %>
              <td><%= f.check_box :next_day,class: "check-box" %></td>
              <td>
                <% if day.started_at.present? && day.finished_at.present? %>
                  <%= working_times(day.started_at,day.finished_at) %>
                <% end %>
              </td>
              
              <td><%= f.text_field :note ,class: "form-control" %></td>
              <td><%= f.collection_select :supporter, User.where(superior: true), :id,:name,{include_blank: true}, class: "form-control" %></td>
            </tr>
          </tbody>
        
      <% end %>  
    </div>  
  </table>
  
  <div class="center">
    <%= f.submit "編集を保存する", class: "btn btn-primary btn-edit-attendances" %>
    <%= link_to "キャンセル",user_path(date: @first_day), class: "btn btn-default btn-edit-attendances" %>
  </div>
<% end %>
